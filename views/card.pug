extends layout.pug

block content
  section#content 

      h2= text
      if hint
        p 
          i hint: #{hint}
      a(href=`${id}?side=${sideToShow}`)= sideToShowDisplay
      br
      a(href='/cards') Next Card